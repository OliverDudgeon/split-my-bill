{
  "extends": [
    "stylelint-config-standard"
  ],
  "rules": {
    "import-notation": "string",
    "at-rule-no-unknown": [
      true,
      {
        "ignoreAtRules": [
          "config",
          "tailwind",
          "layer"
        ]
      }
    ]
  }
}
